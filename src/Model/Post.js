export class Post {
    constructor(id, titre, contenu, auteur, created_at) {
        this.id = id
        this.titre = titre
        this.contenu = contenu
        this.auteur = auteur
        this.created_at = created_at
    }
}